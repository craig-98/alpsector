/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ignore the server folder (has its own package.json)
  webpack: (config, { isServer }) => {
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        fs: false,
        net: false,
        tls: false,
      };
    }
    return config;
  },
};

export default nextConfig;
